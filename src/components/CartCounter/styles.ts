import styled from "styled-components";

export const Container = styled.span`
  position: absolute;
  margin-left: 30px;
  margin-top: -25px;

  background-color: var(--orange);
  border-radius: 50%;
  padding: 1px 8px;
`;